#ifndef CHANGE_NOTIFIER
#define CHANGE_NOTIFIER

/**
 * The change_notifier class is condition_variable that allows
 * threads to receive notifications even if they are not waiting
 * when those notifications are sent.
 */


 //initialize notification_subscriber with unique_lock
 //keep track of subscribers with member function as cunstructor
 //and destructor that calls remove_subscriber

 //subscriber std::list
#endif
